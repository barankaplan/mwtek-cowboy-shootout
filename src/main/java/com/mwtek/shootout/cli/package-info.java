/**
 * Converts command-line arguments into validated, typed application commands.
 *
 * <p>This package understands options such as {@code --seed}, {@code --batch},
 * and {@code --starter}. It also reads a cowboy count when the application is
 * started interactively without arguments; it does not run a shootout.
 */
package com.mwtek.shootout.cli;